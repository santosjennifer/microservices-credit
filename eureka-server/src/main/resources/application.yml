server:
   port: 8761

spring:
   application:
      name: eureka-server
   security:
      user:
         name: eureka-user
         password: usr-eureka
         
eureka:
   client:
      register-with-eureka: false
      fetch-registry: false
